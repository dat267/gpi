# pi (Go) — a faithful port of [pi](https://github.com/earendil-works/pi)

This is a from-scratch Go port of Mario Zechner's [pi](https://github.com/earendil-works/pi)
(`@earendil-works/pi-ai`, `pi-agent-core`, `pi-coding-agent`). The TypeScript
monorepo is **ground truth**: every type, field name, and behavior is ported
from upstream source, not inferred.

## Ground-truth rules

1. **Upstream is the spec.** A port commit must reference the upstream file
   (and commit, when porting a change) it mirrors.
2. **JSON is byte-parity.** Transcripts, session files, and wire payloads
   must round-trip identically to pi's: same key names, same key order, no
   HTML escaping (use `ai.MarshalJSON`, never `json.Marshal`, on anything
   that ends up on disk or the wire). JS object key order is tracked
   explicitly (see `SystemMessage` sections).
3. **JS semantics where pi relies on them.** JS object insertion order,
   `JSON.stringify` behavior, truthiness at API boundaries — divergences get
   a numbered D-row in `docs/PORTING.md` with the reproducing scenario.
4. **Every behavior change is test-observed.** Tests are ported from
   upstream's `test/` files where they exist; Go-only tests must assert the
   upstream-derived expectation, with the upstream source referenced in a
   comment.

## Upstream pin

| What | Value |
|---|---|
| Repository | https://github.com/earendil-works/pi (cloned at `../pi`) |
| Pin | `36b60d2e8` — "fix: clean up delta test lint diagnostics" |
| Stale upstream test | `packages/ai/test/faux-provider.test.ts` "estimates prompt and output tokens" still expects pre-`9e05370b2` faux serialization; source wins |

Update the pin whenever upstream source is re-read for a port.

## Layout (mirrors upstream `packages/`)

| Go package | Upstream | Status |
|---|---|---|
| `ai` | `packages/ai` core (`types.ts`, `utils/event-stream.ts`, `utils/text.ts`, `utils/transcript.ts`, `utils/diagnostics.ts`) | ported |
| `ai` | `packages/ai` models/compat (`models.ts` surface, compat interfaces) | ported (types) |
| `ai/providers` (faux) | `packages/ai/src/providers/faux.ts` | ported |
| `ai` (auth) | `packages/ai/src/auth/*` (types, resolve, credential-store, helpers) | ported |
| `ai` (models runtime) | `packages/ai/src/models.ts`, `models-store.ts`, `api/lazy.ts` (lazyStream) | ported |
| `ai` (catalog) | generated model data via upstream's own generator (`scripts/gen_catalog.py`), `env-api-keys.ts`, `utils/provider-env.ts` | ported |
| `ai` (anthropic wire) | `api/anthropic-messages.ts` request construction (`buildParams`), `transform-messages.ts`, `simple-options.ts`, `constrained-sampling.ts`, `utils/{json-parse,estimate,sanitize-unicode}.ts` | ported |
| `ai` (google-vertex) | `api/google-vertex.ts` — Vertex AI dialect over the shared Google streaming loop: `{location}`-templated and custom base URLs with API-version detection, project/location resolution with upstream's error messages, Vertex API keys (including the ADC marker and `<placeholder>` fallback), Application Default Credentials (authorized-user refresh-token and GCE metadata flows with token caching; service-account keys reported per D27), bearer + `x-goog-user-project` headers, and the Vertex thinking budget/level mapping | ported |
| `ai` (azure-openai-responses) | `api/azure-openai-responses.ts` — Azure dialect over the shared Responses loop: base-URL resolution (options → env → resource name → model base URL) with Azure-host normalization to `/openai/v1`, query stripping only on Azure hosts, `v1` default API version, the `api-version` query parameter, `api-key` header auth, deployment-name resolution (explicit → `AZURE_OPENAI_DEPLOYMENT_NAME_MAP` → model id) sent as the body model, and the Azure tool-call provider set | ported |
| `ai` (mistral) | `api/mistral-conversations.ts` — the native Mistral chat completions endpoint: SDK-to-wire key remapping (maxTokens/toolChoice/promptMode/... and per-message/per-chunk keys), message conversion (string user content, image data URLs with omission notes, assistant thinking parts and tool calls, tool-result text forms), function tools with strict sampling, deterministic 9-character tool-call id normalization, prompt caching via `prompt_cache_key` plus the `x-affinity` header, the CRLF-tolerant SSE event reader with the `[DONE]` sentinel, streaming assembly of text/thinking/tool-call blocks with incremental argument parsing, cached-token usage accounting, reasoning effort vs prompt mode per model, and upstream's error formatting with body truncation | ported |
| `ai/providers` (factories) | `providers/{all,thin factories,opencode-headers,github-copilot,google-vertex,azure-openai-responses,mistral}.ts` — the built-in provider list in upstream order with per-provider base URLs, names, and env API-key auth (28 thin specs plus the anthropic/google/vertex/openai/azure/mistral/copilot factories), the OpenCode session-header wrapper, the GitHub Copilot OAuth available-model filter, a `builtinProviders`/`builtinModels` aggregate, and spec/catalog api consistency | ported (amazon-bedrock, cloudflare, openai-codex, radius factories await their adapters) |
| `ai` (cloudflare) | `providers/{cloudflare-auth,cloudflare-stream}.ts` + the endpoint constants in `api/cloudflare.ts` — per-field credential/env merging for the API key plus account/gateway ids (a key-only credential still picks those up from the environment), the Workers AI key auth vs. the AI Gateway `cf-aig-authorization` form with the SDK placeholder headers suppressed, the login prompts, `{CLOUDFLARE_ACCOUNT_ID}`/`{CLOUDFLARE_GATEWAY_ID}` base-URL materialization before dispatch (cloning only when the URL changes), and both providers registered with the gateway's three-API map pinned | ported (the Workers AI binding fetch is Workers-runtime specific and out of scope) |
| `ai` (oauth) | `auth/oauth/{pkce,device-code,oauth-page,anthropic}.ts` — PKCE (S256) generation, the RFC 8628 device-code polling loop with `slow_down` interval handling and the upstream cancel/timeout/clock-drift messages, the OAuth callback pages (logo, escaping, success/error), the loopback callback server (fixed port, state validation, success/error pages, cancel), manual-paste parsing (URL, `code#state`, query string, bare code), the Anthropic Claude Pro/Max flow (authorize URL, code exchange, refresh with the five-minute expiry margin, upstream error messages, api-key derivation) wired into the Anthropic provider's OAuth slot | ported |
| `ai` (oauth, device code) | `auth/oauth/{xai,kimi-coding}.ts` — RFC 8628 device-authorization flows over the shared poller: the xAI flow (form POST, `referrer=pi`, https-only verification URIs, interval 0 fallback, non-rotated refresh tokens, upstream failure messages per error code) and the Kimi Code flow (host override envs, trusted http(s) verification URIs, 30s request timeouts, raw failure text in messages, refresh with exponential backoff, terminal 401/403/invalid_grant handling), both wired into their providers' OAuth slots | ported |
| `ai` (oauth, openrouter) | `auth/oauth/openrouter.ts` — the OpenRouter PKCE flow: `callback_url` authorize URL, a one-shot loopback server on an ephemeral port with a per-login callback path, 404/409/denied/no-code responses, the JSON key exchange (`code_verifier`, `code_challenge_method`) with a 30s timeout and body-derived error details, the permanent-key credential (no refresh, max expiry), manual-paste handover racing the callback, and upstream's progress/auth-URL notifications | ported |
| `ai` (oauth, github copilot) | `auth/oauth/github-copilot.ts` — the Copilot flow: enterprise-domain prompt/normalization, the GitHub device flow (client id, `read:user`, https-only verification URIs), the Copilot token exchange with the `proxy-ep` -> `api.` base-URL derivation and five-minute expiry margin, the account model catalogue (picker flags, policy states, tool-call support, the Individual-account policy fallback limited to real catalog ids), best-effort policy enabling with a 429-aware retry budget, available-model merging into the credential, and `toAuth` returning the credential-specific proxy base URL | ported |
| `ai` (oauth, openai codex) | `auth/oauth/openai-codex.ts` — the ChatGPT OAuth flow: the authorize URL (extra `id_token_add_organizations`/`codex_cli_simplified_flow`/`originator` params), PKCE + random hex state, the fixed-port loopback server whose listen failure hands over to manual paste, the WHATWG-tolerant JWT decoding used to require a `chatgpt_account_id` claim into the credential, token exchange/refresh with the upstream operation-labelled messages, and the device-code path (string-or-number interval, 403/404 and `deviceauth_authorization_pending`/`slow_down` polling, nested `error.code`) with the device redirect URI | ported |
| `ai` (radius) | `providers/{radius-config,radius}.ts` + `auth/oauth/radius.ts` — gateway URL normalization, the strict catalogue validator (model shape filtering, thinking-level maps), the `/v1/config` loader with body truncation, the dynamic provider (per-provider catalogue cache, stored-entry restore, legacy credential import, network refresh with persisted check timestamps), and the OAuth flow (discovery endpoint, browser sign-in with PKCE/state/`handoff=url` on the fixed loopback port, device-code sign-in with the coded error mapping, refresh, api-key derivation) | ported with the pi-messages API adapter wired |
| `ai` (pi-messages) | `api/pi-messages.ts` — pi's own message protocol: the `{model, context, options}` POST to `<baseUrl>/messages` (context rendered through the canonical message marshaller), optional `debug=1`, bearer + SSE headers, header overrides, the SSE reader (CRLF folding, `data:` lines, `[DONE]`), the event converter assembling text/thinking/tool-call blocks with streaming argument parsing and terminal done/error payloads (usage, response id, provider thinking level), rewrite-impact diagnostics, the coded response error with structured diagnostic details (provider, model, url, status, error object or truncated body), the legacy `PI_CACHE_RETENTION` mapping, and the missing-terminal-event error | ported |
| `ai` (images) | `images-models.ts`, `api/openrouter-images.ts`, `providers/openrouter-images.ts` + the generated image catalogue — the image-generation counterpart of the model runtime (provider auth/model listing/refresh with shared in-flight fetches, generation that resolves auth and merges explicit options per field, error results instead of rejections), the OpenRouter images API (chat completions with `modalities`, data-URL extraction with remote URLs skipped, cache-write/read usage accounting and per-million cost), and `builtinImagesProviders`/`builtinImagesModels` | ported (the catalogue comes from upstream's generated data evaluated with Node's type stripping) |
| `ai` (bedrock plumbing) | AWS SigV4 request signing (canonical request, derived signing key, session tokens, verified against the AWS documentation vector), credential/region/endpoint resolution (explicit env credentials, `~/.aws/credentials`/`config` profile parsing, bearer tokens with `AWS_BEDROCK_SKIP_AUTH`, ARN-embedded regions, standard-endpoint detection and the explicit-endpoint rule, GovCloud detection), Bedrock error formatting with the stable exception prefixes and data-retention hint, failure diagnostics, reserved-header rules, stop-reason mapping, and the AWS event-stream binary decoder (prelude/message CRC validation, typed headers, varints) | ported with the Converse adapter wired |
| `ai` (bedrock-converse) | `api/bedrock-converse-stream.ts` — the Converse adapter: message conversion (text/image/tool-call/thinking blocks with signature replay, redacted reasoning as opaque bytes, the placeholder rules for empty content, consecutive tool results collapsed into one user message, cache points on the system prompt and last user message), tool configuration with strict sampling and forced choices, thinking request fields (adaptive vs budget with the GovCloud display omission, custom budgets, interleaved-thinking beta), the request payload with model/inference/system/tool/metadata fields, the SigV4-or-bearer request, the event-stream fold (messageStart validation, text/thinking/tool-call block assembly with streaming arguments, stop reasons, usage with the one-hour cache split and cost), model predicates (Claude detection, adaptive thinking, native xhigh, prompt caching incl. the force-cache override), and the Bedrock auth (bearer token, AWS profile, credential chain) | ported |
| `ai` (openai-codex) | `api/openai-codex-responses.ts` (SSE transport) — the Codex Responses dialect: ChatGPT account-id extraction from the OAuth token, the request body (instructions instead of a system message, verbosity, encrypted-reasoning include, tool choice/parallel calls, reasoning effort/summary with the model's off mapping, prompt cache key), the Codex headers, the usage-limit friendly message and retry classification with `retry-after`/`retry-after-ms` bounds, the codex event mapping (error/failed events surfaced, terminal statuses normalized to the Codex vocabulary, `end_turn` preserved) feeding the shared Responses processor, the flex/priority service-tier precedence and pricing, and the provider factory. **All upstream providers are now ported** (D28: the WebSocket transport is not; the default `auto` uses SSE, and an explicit `websocket` request is rejected) | ported |
| `agent` | `packages/agent/src/{agent-loop.ts, types.ts, stream-fn.ts}` — the loop, tool execution (sequential/parallel), steering/follow-up queues, terminate rules, validation (D6 subset) | ported |
| `agent` (Agent) | `agent.ts` — stateful wrapper: state, subscribe, steer/follow-up queues (one-at-a-time default), continue semantics, run-failure lifecycle, reset baseline | ported |
| `agent` | the loop and its harness: agent state/queues (steering + follow-up with modes), tool execution (sequential and concurrent modes with per-call lifecycle events), prompt/continue flows, event fan-out with listener settlement, retry classification hooks, compaction/context/execution harness pieces, and the durable run journal integration | ported |
| `coding` (tools) | `core/tools/{truncate,path-utils,file-mutation-queue,read,write,ls}.ts` | ported |
| `coding` (edit) | `core/tools/{edit.ts, edit-diff.ts}` — exact/fuzzy matching, overlap detection, CRLF/BOM preservation, diffs | ported |
| `coding` (search) | `core/tools/{find.ts, grep.ts}` — real fd/rg backends, gitignore respect, relativized output, match/context formatting | ported |
| `coding` (bash) | `core/tools/bash.ts` + `output-accumulator.ts` — bounded-memory streaming, temp-file full output, tree-kill, 128+N signal mapping, timeouts, aborts, PI_* env handling | ported |
| `coding` (sessions) | `core/session-manager.ts` — v3 JSONL trees, branching, compaction context, migrations, discovery/listing; `core/messages.ts` custom roles | ported |
| `coding` (prompt) | `core/system-prompt.ts` (structured sections, forced prompts, section diffing) + `core/skills.ts` + `utils/frontmatter.ts` | ported |
| `coding` (compaction) | `core/compaction/{compaction.ts, utils.ts}` — cut points, token estimation, summarization (retry-wrapped), file-op tracking; `core/messages.ts` convertToLlm | ported |
| `coding` (session facade) | `core/agent-session.ts` (extension-free reduction: persistence, queue tracking, auto-compaction/retry, stats, skill blocks) + `core/defaults.ts` | ported |
| `coding` (powershell + registry) | `core/tools/powershell.ts` (PowerShell variant of the shared shell tool: `-NoProfile -NonInteractive -ExecutionPolicy Bypass -Command` args, UTF-8 console prefix, PS> prompt, pi-powershell temp files, Windows-only exec failure), `core/tools/index.ts` (tool names, `createTool`, coding/read-only/all tool sets), `utils/shell.ts` (bash resolution incl. legacy-WSL stdin transport and Git Bash discovery, PowerShell resolution preferring pwsh, binary-output sanitizing) | ported |
| `coding` (model resolution) | `core/model-resolver.ts` — default models per provider, exact reference matching (bare id or canonical provider/model, ambiguous bare ids rejected), fuzzy partial matching preferring aliases over dated ids, colon-aware thinking-level parsing (OpenRouter-style `model:exacto:high`), scope resolution with glob matching and structured diagnostics, CLI model resolution (provider inference from `provider/model`, authenticated-provider tie-breaks for ambiguous ids, provider/model split preference over gateway ids, custom-id fallback with `:thinking` stripping), initial model priority chain, and session model restoration; `utils/shell.ts`-style glob semantics in `coding/glob.go` | ported |
| `coding` (settings) | `core/settings-manager.ts` + `core/http-dispatcher.ts` timeout parsing — the full settings document (all upstream fields), deep merge (nested objects merge, arrays/primitives replace, undefined skipped), the four legacy migrations (queueMode, websockets, skills object, retry.maxDelayMs), ordered set-aware JSON writing, locked file storage (proper-lockfile semantics) and in-memory storage, scoped managers with global/project trust, reload, overrides, recorded errors, modified-field persistence that preserves externally added keys, and the complete getter/setter surface (models, thinking levels, compaction token resolution, retry, HTTP idle timeouts, editor, shell, resources, terminal/TUI, markdown, warnings) | ported |
| `coding` (cli + json events) | `cli/args.ts` (full flag parser: value flags, shorthands, `--` separator, @file args, unknown-flag capture for extensions, thinking-level validation diagnostics, session-name normalization, help text) and `modes/json-event.ts` (streaming `message_update` events stripped of cumulative partials, toolcall_start enriched with id/toolName, cumulative usage preserved, passthrough payloads for the other event types) | ported |
| `coding` (context files) | `core/resource-loader.ts` context discovery + `core/footer-data-provider.ts` git path resolution — AGENTS.override.md/AGENTS.md/AGENTS.MD/CLAUDE.md/CLAUDE.MD preference per directory, directory candidates skipped, BOM stripping, global agent file first then ancestors outermost-to-innermost with path dedup, and the nested-worktree shadow rule (commondir/worktree detection, bare-layout and submodule exclusions) | ported |
| `coding` (prompt templates) | `core/prompt-templates.ts` + `core/source-info.ts` — bash-style command argument parsing (including the implementation's literal-backslash quote behaviour), the full placeholder vocabulary ($N, $@, $ARGUMENTS, `${N:-default}`, `${@:-default}`, `${@:N}`, `${@:N:L}`) with non-recursive substitution, template loading from global/project/explicit paths (markdown only, symlink-to-file resolution, broken symlinks skipped), frontmatter description/argument-hint with the first-line truncation fallback, source info (user/project/temporary, top-level origin), and `/name args` expansion | ported |
| `coding` (file args + mime) | `cli/file-processor.ts`, `utils/mime.ts`, and the inline-image limits from `utils/image-resize*.ts` — @file argument expansion (path resolution, empty files skipped, missing/unreadable files erroring, BOM-stripped text wrapped in `<file name>` blocks), image sniffing (JPEG with the JPEG-2000 marker rejected, PNG with IHDR validation and animated-PNG detection, GIF, WebP, structurally validated BMP), and inline-image preparation (MIME normalization, byte-identical pass-through within the 2000x2000/4.5MB limits, conversion hints, dimension notes, upstream omission messages) | ported |
| `coding` (print mode) | `modes/print-mode.ts` (extension-free core) — JSON mode writing the session header before one JSON line per event, text mode printing the final assistant message's text blocks, the error/aborted stop-reason path reporting to stderr with exit code 1, initial images reaching the provider, sequential prompts, and prompt-failure exit handling | ported |
| `coding` (session runtime, complete) | the remaining core surface: agent-session state/control (tools, queue modes, model/thinking management, scoped models, auto toggles, bash recording), the prompt pipeline (expansion, streaming behaviors, auth validation, deferred messages), the auto-retry loop with the post-run continuation, automatic compaction (overflow/threshold/recoverable-length via the shared overflow detector), branch summarization + navigateTree, session bash execution + manual compaction, cache warming (economics, safety windows, persisted usage), the model runtime + registry facade, config-value resolution, provider attribution + runtime credentials, remote catalog overlay, auth.json storage + models store, HTML entity decoding + tool-result image normalization, JSONL export + the bug report (redaction, metadata, diagnostics, summary) + crash log, project trust, git branch/deprecation helpers, and slash commands | ported |
| `coding` (out of scope) | exportToHtml (needs the interactive TUI theme resolution chain), the resource loader / sdk / agent-session services and runtime (extension mechanics, D41), the package and tools managers, and the TUI itself (modes: interactive TUI, rpc runner) | out of scope |
| `protocol` | `packages/protocol` — strict definite-length CBOR subset with limits, 4-byte length framing, v8 message schemas (hello/request/cancel/response/service_update/attachment), strict validation codecs | ported |
| `client` | `packages/client` — transport contract, connection state machine (handshake validation, sequence fencing, fail/close semantics), requests with cancel-on-abort, attachment routing, state listeners, service catalogue + subscriptions (hydration, queued updates, start gating, dispose), Chord service transport, dispose | ported |
| `chord/services` | `packages/chord/src/services/{wire,state-codec,errors}.ts` — control calls, strict snapshot/update validation, per-subscription state codec registries, remote-service error codes | ported |
| `chord/delta` | `packages/chord/src/delta` — op vocabulary + wire compression, diff engine, applier (mutable/immutable), validation; JSON value contract (`chord` root) | ported |
| `telemetry` | `packages/telemetry` — span/context contracts, noop + in-memory recorder (settlement, explicit-vs-automatic status, atomic recording), schema definition data, typed span starter, and the adapter conformance suite | ported |
| `durable` | `packages/durable` — conversation/entry/task/input/document record types, the Storage atomic persistence contract, and the detached in-memory reference storage (commit sequences, id ownership, cursors, fork-aware history, task/input indexes) | ported |
| `server` | `packages/server` core — connection state machine (handshake timeout/version checks, hello_error failures, inbound pump ordering), request dispatch (service-call parsing, session vs server-scope routing, subscribe snapshot encoding with update flush ordering, unsubscribe, cancel), SessionRouter (per-client serialization, attachment leases, session open dedup, terminate invalidation), bounded error mapping, drain/close | ported |
| `server` (unix) | `transports/unix/*` — socket path derivation, listener with stale-socket probing (live-listener refusal, rename-and-verify removal), atomic bind path + hard link + mode, owned-socket cleanup by device/inode identity, per-connection byte accounting, graceful final-chunk close with force-close timer, `createUnixServer` preset | ported |
| `server/testing` | `testing/*` — `Deferred`, scriptable `TestHarness` (gated close/service calls, release counting), `TestServerHost` (seeded metadata, ambiguity, gated opens), `pi.session-management` test services, `ProtocolTestClient` with fragmented sends and attachment tracking, `connectUnixTestClient`, `createTestServer` | ported |
| `chord/facets` | `facets/{host,loader}.ts` + the facet surface of `api.ts` — `FacetKernel` (facet setup with setup/active/disposing/dead lifecycle guards and revoking service access, provision recording and shape comparison, remote+local+deferred service-source resolution with duplicate-offer rejection, provider/internal-binding/local-keyed assembly, dependency validation with mode and ownership checks, topological activation order with cycle detection, connect/ready gating, reload with staged candidates, replacement validation, cutover, retirement and abort, terminate/dispose error collection), `HostServiceSlots` (per-service slots with late-binding guarded views), the keyed-spawner staging used by reload, `LocalKeyedServiceRegistry`, and the static/combined facet loaders | ported |
| `chord/services` (provider/consumer) | `services/{provider,consumer,handle,loopback,errors}.ts` — `RemoteServiceProvider` (catalogue validation, singleton provide/withdraw/validate/replace with member-shape preservation, keyed spawn/generation/close, coded invoke errors, subscriptions with pre-activation buffering, dispose), `createRemoteServiceEndpoint` (catalogue/subscribe/unsubscribe control calls, update publishing, subscription disposal), `createLoopbackServiceTransport`, and the consumer `RemoteServiceBinding` (allowlist + mode exclusivity, facade member handles with kind tracking and access guards, singleton snapshots/replacement/unavailable, keyed instance directory with guarded observations, ready gating, rebind, dispose) | ported |
| `chord/services` (replicated state) | `services/{state,state-internals,instances}.ts` — host-owned mutable state (deep-clone initial publication, diff-based publish, op-batch source listeners, hydrate-then-update deliveries), the cold consumer replica (base-batch-only hydration, sequence-gap clearing, listener-failure reporting), the internals lookup, and the keyed instance directory (generation identity, readiness gating, cancellable per-instance observation tasks, reset/dispose) | ported |
| `chord` | `packages/chord` — root state container (host-owned state, ops, facets), delta ops with the wire tuples, services (provider/consumer/state/instances), and facets | ported |
| `chord` (node bundler) | esbuild/rolldown JS facet bundling — JavaScript-runtime specific | out of scope |
| `tui` (foundation) | `@earendil-works/pi-tui` src/utils.ts — grapheme-cluster segmentation (combining marks, ZWJ sequences, skin tones, regional-indicator flags, indic virama joining), terminal-cell width (CJK/fullwidth double via the Unicode east-asian-width tables, emoji presentation, tab = 3, ambiguous = 1), terminal-sequence extraction (CSI/OSC/APC with BEL and ST terminators) and stripping, and the width cache | ported |
| `tui` (line primitives) | src/utils.ts — `truncateToWidth` (contiguous-prefix kept-width rule, ellipsis budget, padding only when the text fit), `sliceByColumn`/`sliceWithWidth` (visible-column extraction with pending-ANSI prefixing), `extractSegments` (before/after with inherited styling at the boundary), `AnsiCodeTracker` (SGR attribute/256-color/RGB state, OSC 8 hyperlinks with their original terminator, line-end reset), `wrapTextWithAnsi` (token-based wrapping with style carry-over, long-token breaking, CJK breaking), `applyBackgroundToLine`, `normalizeTerminalOutput` (Thai/Lao AM decomposition, tab expansion) | ported |
| `coding/interactive` (leaf components) | `modes/interactive/components/{keybinding-hints,countdown-timer,dynamic-border,visual-truncate,bordered-loader,markdown-transform,user-message,custom-entry}.ts` plus the theme-derived factories (`getMarkdownTheme`/`getSelectListTheme`/`getEditorTheme`/`getSettingsListTheme`/`getResolvedThemeColors`/`getThemeExportColors`/`isLightTheme`/`getLanguageFromPath`/`highlightCode`): key text/display formatting with the macOS Option rule, the key/raw hints, the countdown timer, the dynamic border, visual-line truncation (including the JS slice(-0) quirk), the bordered loader (cancellable and plain), the markdown transformer pipeline, the user-message component with OSC 133 zone markers, and the custom-entry component with the expanded state and the renderer-failure fallback box | ported (D74: highlighting falls back to per-line code styling; D85: ActiveTheme accessor; D86: style-color switch) |
| `coding/interactive` (scaffolding) | `modes/interactive/{chat-viewport,theme/theme-controller,model-search,external-editor,model-catalog-refresh}.ts` — the shared chat viewport (follow-end primary transcript + the fixed input dock with pending/status/widgets/editor/footer and their stretch rules), the settings-driven theme controller (auto light/dark settings, terminal color-scheme syncing, preview, in-memory instances, error fallback to dark), the model search-text builders, the external editor launcher (temp file, BOM/trailing-newline handling, failure status), and the model-catalog refresh coordinator (per-runtime deduplication with independent caller cancellation) plus the terminal query detection helpers | ported (D76: sequential terminal queries instead of concurrent; D77: Go waits on the child process directly) |
| `coding/interactive` (theme) | `modes/interactive/theme/{theme,theme-json}.ts` — the theme document shape with structural validation and the missing-color summary, the color utilities (hex parsing, the 6x6x6 cube and grayscale 256-color conversion with the saturation guard, foreground/background ANSI building for truecolor and 256-color modes), variable resolution with circular/missing-reference errors, the optional-token fallbacks, the `Theme` palette (fg/bg maps, `fg`/`bg` with partial resets, chalk-equivalent bold/italic/underline/inverse/strikethrough, raw ANSI accessors, thinking-level and bash-mode border colors), built-in dark/light loading from the embedded upstream JSON, custom theme discovery/loading, the terminal light/dark detection (COLORFGBG index luminance, RGB luminance, fallback), the auto-theme setting parse/resolve, and the global theme registry with watcher | ported (D74: highlight.js syntax highlighting out of scope, so the markdown renderer's fallback applies; D75: the theme file watcher polls instead of fs.watch) |
| `tui` (autocomplete provider) | src/autocomplete.ts — `CombinedAutocompleteProvider`: slash-command completion with fuzzy filtering (including the skill: prefix rule), argument completion through command hooks, quoted/`@`-prefixed token extraction, path completion for relative/absolute/`~` prefixes with the root/trailing-slash/file-prefix split, directory-first ordering, symlink-to-directory resolution, completion-value quoting, `applyCompletion` for commands/attachments/arguments/paths with the directory-without-space and trailing-quote cursor rules, fd-based fuzzy file search (base-directory first then recursive, scoring with the directory bonus, depth/length/name tie-breaks, the 20-item cap, `.git` filtering, scoped queries), and the Tab file-completion trigger | ported (D72: ICU collation approximated by case-insensitive comparison; D73: command entries are a single type instead of a SlashCommand/AutocompleteItem union) |
| `tui` (editor) | src/components/editor.ts (plus the autocomplete surface from src/autocomplete.ts) — the multi-line prompt editor: the paste-marker aware segmenter and word wrapper, the scroll-indicator borders, the word-wrapped layout with cursor mapping, the visual-line map and sticky-column vertical movement with atomic-segment snapping, grapheme-accurate insert/delete/word deletes/line deletes, the kill ring with yank/yank-pop, undo snapshots with paste-registry restore, prompt history browsing (draft restore, multi-line entries), bracketed-paste handling with the large-paste marker registry and renumbering, character jump mode, mouse cursor placement including wrapped lines, the render pipeline (padding, fake cursor, hardware cursor marker, autocomplete list and scrolling), and the autocomplete integration (trigger patterns, slash-command context, Tab/force completion, suggestion list wiring) | ported (D71: synchronous provider call under the editor lock instead of debounce/abort; the CombinedAutocompleteProvider implementation lands next) |
| `tui` (alt-screen renderer) | src/tui-alt-screen.ts — the fullscreen renderer: enter/exit sequences with mouse-mode selection and capability handling, the layout-frame render pipeline (search refresh, OSC 133 stripping, search highlights, scroll-to-end indicator, overlay compositing, viewport clipping, selection painting, flashes, cursor extraction, over-wide line clamping), the screen diff with full/partial redraws and cursor positioning, the deferred-document replay on stop, viewport scrolling (keys, prompts, top/bottom), search toggle/query/navigation with reveal, mouse handling (SGR/legacy parsing, overlay and layout dispatch with layout-node skipping, capture/drag/click with double/triple-click word/line selection, auto-scroll, scrollbar hover/drag, right-click paste, OSC 8 URL activation, wheel routing with overscroll chaining), selection text extraction and OSC 52/injected clipboard copy with flashes | ported (D81: kitty image caching omitted; D82: selection auto-scroll timer; D83/D84: auto-render disable seam and render serialization) |
| `tui` (alt-screen search) | src/alt-screen-search.ts — the fullscreen transcript search: the corpus builder (the ASCII fast path indexing non-space runs with linear column mapping, and the grapheme path for ANSI/wide/CJK text), whitespace-normalized case-insensitive matching with the span-to-segment projection and adjacent-segment merging, the cached search index (source/query change detection), the match key, and the search overlay component (input + result counter + navigation buttons with the keybinding-derived labels, hover state, responsive button layout, and button hit-testing) | ported (D80: the nav-direction and hover APIs use (value, ok) instead of undefined) |
| `tui` (main-screen renderer) | src/tui-main-screen.ts — the differential renderer for the main screen and scrollback: the state capture/restore, the bounded 1 MiB output writer with synchronized-output framing, the full-render paths (first render, width/height changes with the Termux height exception, clear-on-shrink) and the incremental paths (first/last changed line detection, append starts, the all-deleted-lines clearing path, viewport-relative scrolling, per-line clear + write, tail clearing with cursor restoration), the hardware-cursor positioning for IME (row deltas + absolute column), the kitty image header parsing with reserved-row blocks and deletion of changed images, the over-wide-line crash log, and the render hooks | ported (D78: rune-aware chunking instead of surrogate pairs; D79: over-wide lines report through a hook instead of throwing) |
| `tui` (latex) | src/latex.ts — the LaTeX math-to-Unicode renderer: the extracted symbol/command tables (224 symbols, negated symbols, blackboard letters, super/subscript digits, accents, named operators, limits, relation commands, spacing/font/size/ignored/plain-wrapper sets), the script formatter (unicode scripts with the `_`/`^(...)` fallback and the `=`/`+-` normalization), fraction/root formatting, the named-operator output normalization, the LaTeX parser (sequences, commands, required/optional/raw arguments, `\limits` modifiers, operator limits, environments: equation/aligned/gather/multline/split/cases/array/matrices with delimiters), and the layout engine (fraction stacking, operator limits, script stacking, matrix rows with padding, baseline joining, marker-based multi-line composition) | ported (D69/D70: capabilities injectable, lookaround-free matching) |
| `tui` (markdown renderer) | the renderer half of src/components/markdown.ts — the `Markdown` component (cache, padding, default text style with the colour/background split, the sentinel-based style-prefix reapplication), block rendering (headings with level-specific styling and the #≥3 prefix, paragraphs, code blocks with optional highlighting and the closing-fence trim, quotes with border and style reapplication, hr, html, space tokens), the inline renderer (nested strong/em/del/codespan/link/html/latex with the style-prefix trailing trim), lists (nesting, markers, preserve-markers mode, task markers, continuation indentation, loose spacing), and tables (natural/min column widths, the shrink/grow distribution with rounding fixups, cell wrapping with the mid-fragment style reset, borders) plus the OSC 8 `Hyperlink` helper and injectable terminal capabilities | ported (D68: src/latex.ts is a separate port; RenderLatex currently reports unsupported, which is upstream's raw-source fallback path; D69: capabilities are injectable) |
| `tui` (markdown parser) | the tokenizer half of src/components/markdown.ts — a Marked-compatible block + inline lexer producing the token tree the renderer consumes: headings (ATX, multi-line setext, levels, inline tokens), paragraphs, fenced (backtick/tilde, language) and indented code, blockquotes, ordered/unordered lists with nesting, task markers, tight/loose shapes, tables (alignment, header, rows), hr, html blocks, space runs, the strict strikethrough rule, escape/entity handling, autolinks and inline links/images, hard breaks, intraword-underscore emphasis rules, and the LaTeX block/inline extensions plus `trimPartialClosingFences` | ported (D67: Go has no `marked` dependency available, so this is a built-in subset lexer verified against upstream Marked token projections for a 79-case corpus) |
| `tui` (fuzzy + settings list) | src/fuzzy.ts (subsequence matching with the consecutive/boundary/gap scoring, the letter-digit swap retry, exact-match bonus, and token filtering over whitespace/slashes with a stable sort) and src/components/settings-list.ts (settings rows with label/value alignment, the 36-column label clamp, value truncation, scroll indicators, the selected-item description wrapping, the optional fuzzy search input, Enter/Space value cycling, submenus with the done callback including navigate-to re-entry, mouse wheel/press/click handling with the search-row offset, and the hint line) | ported (D66: the Go matcher function is MatchFuzzy because the type and upstream function names collide) |
| `tui` (list + text + loader components) | src/components/select-list.ts (value-prefix filtering, visible-range scrolling centred on the selection, the primary/description two-column layout with column-width bounds and custom truncation, scroll indicators, wheel/press/click mouse handling with press-then-click confirmation, wrap-around keyboard navigation), src/components/truncated-text.ts (single-line truncation with padding), src/components/loader.ts (frame animation with the verbatim/coloured indicator and message updates), src/components/cancellable-loader.ts (abort on cancel with a done channel), src/components/mouse-region.ts (child-first mouse delegation), src/components/alt-screen-flash.ts (timed transient messages) | ported (D62 render-requester surface, D63 done channel instead of AbortSignal, D64 stopped flag for the animation callback) |
| `tui` (input + box + keybindings) | src/keybindings.ts (the built-in keybinding table with descriptions, the manager with user overrides, duplicate-key normalization, conflict detection, resolved bindings, and the global accessor), src/kill-ring.ts + src/undo-stack.ts (Emacs kill/yank ring with accumulate/prepend/rotate; snapshot stack), src/word-navigation.ts (backward/forward word navigation over word segments), src/components/box.ts (padding + background container with the bg-sample cache, mouse forwarding, background padding), src/components/input.ts (single-line input with the fake cursor and hardware-cursor marker, horizontal scrolling with the end-cursor column reservation, bracketed-paste buffering, kill/yank/yank-pop, undo with word coalescing, grapheme-aware movement and deletion, Kitty CSI-u printable decoding, control-character rejection, placeholder rendering, mouse cursor placement) | ported (D58 word segmentation heuristic, D59 no deep clone, D60 canonical binding order, D61 byte offsets) |
| `tui` (layout engine + scroll view) | src/layout.ts (the box-tree layout pass: leaf/scroll/vstack/hstack boxes, the render cache, clip intersection, scroll translation with follow-end, the measured-versus-allocated sizing, the line-offset rule for cursor markers beyond the allocated height, the cursor-marker-aware line offset), src/components/scroll-view.ts (scroll position/follow-end state machine, scrollTo/scrollBy/scrollToStart/scrollToEnd, the transient auto scrollbar with its hide timer, the always-mode content-width reservation, the single-child invariants), the screen painter (recursive box painting with the full-width fast path and OSC 133 zone stripping, scrollbar cell replacement preserving the target background, the scrollbar geometry with thumb sizing and offsets), and the hit-testing helpers (getLayoutBoxesAt with layer/depth ordering, getScrollViewBox, getScrollViewsAt) | ported (kitty image metadata/cropping stubbed out of scope: D57) |
| `tui` (layout foundation + basic components) | src/layout-node.ts (the layout-node provider with stack/scroll nodes), src/components/stack.ts + v-stack/h-stack (the flex sizing algorithm: basis/min/max clamping, weighted grow and shrink distribution with capacity limits, gap accounting, the visible callback, the stack component with entry bookkeeping), src/components/text.ts (word-wrapped padded text with the width-aware margin reduction, tab expansion, optional background, cache) and src/components/spacer.ts | ported (upstream goldens for the sizing algorithm, stacks, and text) |
| `tui` (keys) | src/keys.ts — the `KeyId`/`Key` identifier surface, the legacy sequence tables (ANSI/SS3/PC-style function keys, shift/ctrl variants, alt+arrow), `matchesKey` for specials, arrows, function keys, and letter/digit/symbol keys across the legacy, Kitty CSI-u, and modifyOtherKeys encodings (base-layout-key fallback for non-Latin layouts with the remap guard, the raw-0x08 Windows Terminal heuristic, mode-aware `\x1b\r`/`\n` mappings), `parseKey` formatting (`formatParsedKey` with functional-codepoint normalization, shifted-letter identity, and lock-mask handling), `decodeKittyPrintable`/`decodePrintableKey` for plain/Shift CSI-u and modifyOtherKeys input, the key-event type tracking, and the Kitty protocol global state; verified against an upstream-generated golden matrix (169 inputs x 2 protocol states x 85 key ids plus parse/decode results) | ported |
| `tui` (terminal + stdin) | src/terminal.ts + src/stdin-buffer.ts — `StdinBuffer` (complete-sequence extraction for CSI/OSC/DCS/APC/SS3/meta with the SGR-mouse shape check, old-style ESC[M 6-byte events, the WezTerm ESC+ESC+CSI split, the escape vs sequence timeout, bracketed-paste re-wrapping, and the Kitty printable raw-echo dedup), `ProcessTerminal` (raw mode via x/term, bracketed paste on/off, SIGWINCH resize watcher, columns/rows with COLUMNS/LINES fallbacks, cursor/clear/title/OSC 9;4 progress with keepalive, drainInput idle protocol, the PI_TUI_WRITE_LOG output log), the Kitty keyboard protocol negotiation (flags request with the DA sentinel, modifyOtherKeys fallback, split-response buffering with the 150 ms flush timer), `resolveEscapeTimeoutMs` (PI_TUI_ESC_TIMEOUT / SSH), and the keyboard-protocol globals from keys.ts | ported |
| `tui` (renderer core) | src/tui.ts — Component model with the optional-method interfaces (InputHandler/MouseHandler/KeyReleaseWanter/Focusable), mouse event normalization with `dispatchMouseEvent`/`retargetMouseEvent`, `Container` (render + mouse-layout hit testing), the overlay stack (anchors/percent/margin layout, focus-order stacking, capture/non-capturing modes, handles with hide/setHidden/focus/unfocus/bounds, focus-restore state machine), `compositeTuiLine`/`compositeOverlays` (screen-relative compositing with segment resets), `applyLineResets`, cursor-marker extraction, and the render scheduler (16 ms throttle, immediate-render preemption of queued throttled frames); src/keys.ts `isKeyRelease`/`isKeyRepeat` | ported (terminal.ts, tui-main-screen/tui-alt-screen doRender, and the terminal query protocols pending) |

## Final scope assessment

Every upstream runtime package is accounted for: `ai` (all ten wire APIs, all provider factories, the seven OAuth flows, image generation, the api/images registries, overflow detection), `agent` (the loop), `coding-agent` (the complete core: session, tools, compaction, branch summarization, retry, cache warming, model runtime/registry/composer, stores, exports, bug report, crash log, trust, utilities, and the createAgentSession assembly), `protocol`, `client`, `chord` (+delta/services/facets), `server` (+unix/testing), `telemetry`, and `durable`.

The TUI port is in progress: the width foundation, line primitives, and renderer core are ported; terminal.ts, the screen renderers, layout, and components follow.

Deliberately out of scope, with divergences recorded in code (D1-D41): the interactive TUI and its support modules (keybindings, output guard, footer data, exportToHtml's theme chain), the extension mechanics (resource loader, sdk extension surface, agent-session services/runtime, package and tools managers), the node-specific chord bundler, the bug-report upload transport, and the optional `session-backends` sqlite driver (a host-provided storage backend imported by no runtime package) plus the `evals` Docker harness (a development tool).

## Build & test

```bash
go build ./...
go test -race ./...
```
